# SFT 工作指导与远程开发指南

## 1. 任务背景分析
你的任务是**复现/迁移** ORANSight 项目的能力。
-   **目标**：将 ORANSight (基于 Llama/Qwen 等稠密模型) 的能力迁移到 **MoE 架构 (Qwen3-30B-A3B)** 上。
-   **核心逻辑**：利用开源的“数据”和“代码”，在 **Qwen3-30B-A3B** 上进行 **SFT (有监督微调)**，使其具备 O-RAN 领域的知识，同时享受 MoE 架构带来的推理加速和低显存占用优势。

## 2. 核心疑问解答

### Q1: 为什么 0.5B 的训练代码可以通用于 30B？
**因为你用的是框架 (LLaMA-Factory/Transformers)，而不是手写矩阵乘法。**

*   **比喻**：想象你在学开车。
    *   **0.5B 模型**：是一辆玩具卡丁车。
    *   **30B 模型**：是一辆载重卡车。
    *   **你的代码**：是“驾驶技术”（踩油门、打方向盘、挂挡）。
    *   **结论**：你在卡丁车上练熟了“听到指令就左转”的操作，这套操作逻辑在开卡车时是**一模一样**的。
    *   **唯一需要改的**：仅仅是配置文件里的 **`model_name_or_path`** 这一行字符串，以及因为显存限制而调整的 **`batch_size`**。其他的代码（数据处理、训练循环、保存逻辑）完全通用。

### Q2: Docker 的“环境配置”到底去哪了？ (为什么不用自己装？)

你可能会问：*“我在本地都要 pip install 半天，为什么服务器上一行命令就好了？”*

**答案是：有人帮你装好了，并且打包成了“镜像”。**

*   **比喻：网吧电脑还原卡 / 游戏卡带**
    *   **Image (镜像)**：这就好比网吧老板（LLaMA-Factory 开发者）早就把一台电脑装好了 Windows、显卡驱动、PyTorch、各种依赖库，然后做成了一个**“系统还原盘”**。
    *   **Docker Pull**：当你运行 `docker pull hiyouga/llama-factory:latest` 时，你就在师兄的服务器上下载了这个“完美配置好的系统还原盘”。
    *   **Docker Run**：当你运行这行命令时，相当于**瞬间开机**进入了这个完美系统。里面什么都有，根本不需要你再去 `pip install`。

### Q3: `/app/my_code` 到底是谁的文件夹？ (挂载映射详解)

*   **`/app/my_code`**：是**容器内部**看到的路径（Container Path）。
*   **`/home/shixiong/My_SFT_Project`**：是**师兄服务器上**的真实路径（Host Path）。

命令中的 `-v` 参数起到了**“虫洞/传送门”**的作用：

```bash
# 格式：-v 外部真实路径:内部虚拟路径
-v /home/shixiong/My_SFT_Project:/app/my_code
```

*   **这意味着什么？**
    *   你在容器里面访问 `/app/my_code`，**实际上**读写的就是师兄服务器硬盘上的 `/home/shixiong/My_SFT_Project`。
    *   它们是**同一个文件夹**，只是在墙里墙外叫法不一样。
    *   **如果不挂载**：容器里就是空的，你的代码就塞不进去。

---

## 3. 具体操作步骤：从本地到上机

### 第一阶段：本地准备 (Artifact 制作)
在你的 Windows 电脑上，整理出一个干净的文件夹，假设叫 `My_SFT_Project`。

**文件夹结构如下：**
```text
My_SFT_Project/
├── data/                    <-- 你转换好的 JSON 数据
│   ├── oran_train.json
│   └── dataset_info.json    <-- 注册数据配置
├── config/                  <-- 训练配置
│   ├── train_config.yaml    <-- 你的微调参数 (LR, Epochs)
└── run.sh                   <-- 启动脚本
```

**你的 `run.sh` 脚本大概长这样：**
```bash
# 这是一个在容器内部运行的脚本
llamafactory-cli train config/train_config.yaml
```

**你要做的“打包”动作**：
只是右键点击 `My_SFT_Project` -> **压缩为 My_SFT_Project.zip**。

### 第二阶段：传输与部署
1.  **发送**：把 `My_SFT_Project.zip` 发到师兄服务器。
2.  **解压**：在服务器上解压，得到 `/home/shixiong/My_SFT_Project`。
3.  **下载环境 (只需做一次)**：
    师兄服务器执行：`docker pull hiyouga/llama-factory:latest`。

### 第三阶段：一键启动 (Docker Run)详解

在服务器终端输入（这是你需要记下的一行核心命令，我们逐行翻译）：

```bash
docker run --gpus all \                          # 1. 允许容器使用服务器的显卡
    --shm-size 16G \                             # 2. 给内存交换空间，防止卡死
    -v /home/shixiong/My_SFT_Project:/app/my_code \  # [映射] 服务器的真实文件夹 -> 容器里的虚拟文件夹
    -v /data/models/Qwen3-30B:/app/model \           # [映射] 服务器的模型文件夹 -> 容器里的模型文件夹
    -w /app/my_code \                                # 告诉容器：一进去就站在这个目录里
    -it hiyouga/llama-factory:latest \               # 使用镜像
    bash run.sh                                      # 运行命令
```
