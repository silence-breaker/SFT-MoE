# O-RAN 领域专属 MoE 大模型微调技术路线方案

**汇报人**：危靖 **日期**：2024年1月 **目标模型**：Qwen3-30b-a3b (MoE架构) **硬件资源**：NVIDIA A100 GPU × 8

## 1. 项目背景与目标

本阶段工作旨在将 ORANSight-2.0 在 O-RAN 领域的知识能力，迁移并内化至更适合端侧推理的 **MoE (Mixture of Experts)** 架构模型中。 

## 2. 核心技术策略：从“适配”转向“重塑”

基于现有算力评估，AI的建议是采用以下高配微调策略：

- **训练模式**：**Full Fine-Tuning (全量微调)**
    
    - _决策依据_：相比仅训练 Adapter 的 LoRA 方案，全量微调能深入调整 MoE 模型的所有专家（Experts）和路由（Router）权重，确保模型深刻理解 O-RAN 复杂协议，而非浅层记忆。
        
- **计算精度**：**BF16 (Bfloat16)**
    
    - _决策依据_：利用 A100 的 BF16 硬件特性，相比 FP16 能提供更宽的动态范围，有效防止训练过程中的梯度溢出（Loss NaN）问题，保障训练稳定性。
        
- **并行策略**：**DeepSpeed ZeRO-3**
    
    - _决策依据_：通过 ZeRO-3 技术将 30B 模型的参数、梯度和优化器状态切片分布到 8 张显卡上，解决单卡显存瓶颈，实现显存利用率最大化。
        

## 3. 目前问题
1. 论文中RANSTRUCT源代码和训练集并没有开源，只有基准测试集。作者在相关文档中表示，他们“正在致力于公开发布 ORANSight，包括相关代码，并且很快会分享一个可用的应用程序”。（或许可以逆向生成它们的RANSTRUCT框架）
2. 部署到FPGA上的时候是否需要RAG和向量数据库，这意味着我们是否一定要采用全量微调